# In the following binary tree array representation, which value would be the left child of the 51?
# 51, 22, 79, 3, 26, 64, 103
# 22

# 1, 1, acc
# 0, 0...0, 1, 1, acc
# * in regex means 0 or more
# + in regex means 1 or more
# /w in regex means any word character
# /d in regex means any digit character
# /s in regex means any whitespace character
# space in regex means a space character
